多类环境训练

1. 预先指定训练环境类别及其范畴

2. 不同类别的环境使用不同的
    
    - reset 策略
    - termination 判定条件 -> 重新实现 termination class
    - rewards 计算方式
    - events -> 
    - critics (待定)
    - reference motion （待定）

3. commands 作为接口和流程管理器

    - 包含注册式的模块
    - 模块间共享的 tensor
    - 对外暴露的 tensor

    - timeline -> 只算 id 转换
    - adaptive sampling -> 设计数据采样技巧，保障训练的合理分布动态
    - selector -> 根据不同训练模式，采样跟踪动作
    - resetter -> 根据 sampling motion frame 初始化环境
    - cache    -> 负责多样的动作对齐机制
    - metrics  -> 负责监控指标，dump 过程文件

    流程：
    # TODO 当前的流程不利于 GPU 并行化计算
    # 系统会自动先调用 _resample_command 然后调用 _update_command
    - rewards, termination finished
    - _resample command
        # NOTE 这里更新 terminated 的环境
        - selector
        - timeline
        - resetter
        - cache alignment
    - _update_command
        - update step ++
        - update motionlib
        # NOTE 这里更新 expired_env_ids (步数超出 motion sequence 的情况)
        - _resample command
            - selector
            - timeline
            - resetter
            - cache alignment
        - cache alignment
        - update failed counts

    # 设计的新流程
    思路：先计算 expired_env_ids 与 terminated_env_ids 合并，然后进行计算
    

边界定义：
    
    对于 MDP 的组成模块，其应该只可以显式调用 env 中的信息
    不可以在未声明的情况下在函数中直接使用


4. 如何减小环境计算分叉导致的 GPU 串行的问题
