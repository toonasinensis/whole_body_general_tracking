多类环境训练

1. 预先指定训练环境类别及其范畴
2. 不同类别的环境使用不同的
    
    - reset 策略
    - termination 判定条件 -> 重新实现 termination class
    - rewards 计算方式
    - events -> 
    - critics (待定)
    - reference motion （待定）

边界定义：
    
    对于 MDP 的组成模块，其应该只可以显式调用 env 中的信息
    不可以在未声明的情况下在函数中直接使用


3. 如何减小环境计算分叉导致的 GPU 串行的问题

4. PipeLine 

- Commands 产生 reference motion frame ID 
- 根据 motion frame ID 计算其他数据的状态
- 根据 motion frame ID， 重置机器人状态
- 施加 events
- 计算 rewards
- 计算 termination 条件
- 根据 failures 计算 sampling probabities

共享变量
- motion frame global id
- motion frame local id
- motion chunck id
- 环境分类的 env id 段: {name: bool_sequence}

将 Commands 看作一个特殊包装的 CommandTerm 